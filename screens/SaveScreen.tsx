import React from "react";
import { Text, View } from "react-native";

interface SaveScreenProps {}

const SaveScreen: React.FC<SaveScreenProps> = ({}) => {
  return (
    <View>
      <Text>Save Screen</Text>
    </View>
  );
};

export default SaveScreen;
